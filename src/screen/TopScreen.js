import React, { Component } from 'react';
import { View, Text, Aler, Button } from 'react-native';

class TopScreen extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> Top Screen </Text>
      </View>
    );
  }
}

export default TopScreen;
